%h2 買い物カゴ

%p 以下の商品が買い物カゴに追加されました

%p
  = @product.name

%ul
  - @cart.items.each do |item|
    %li
      = item.name
      &times;
      = item.quantity

= button_to "買い物カゴを空にする", :action => "empty_cart"

